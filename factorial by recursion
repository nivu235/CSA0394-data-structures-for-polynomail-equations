#include<stdio.h>
int fac(int n);
int main()
{
	int i,n,fact=0;
	printf("enter the number");
	scanf("%d",&n);
	fact=fac(n);
	printf("factorial of %d is %d",n,fact);
	
}
int fac(int n)
{
	if(n==0)
	{
		return(1);
		
	}
	else
	{
		return(n*fac(n-1));
	}
}
